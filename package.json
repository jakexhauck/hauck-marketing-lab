{
  "name": "hauck-command-center-workspace",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "pnpm@10.18.0",
  "description": "Workspace root for the Hauck Command Center (desktop + shared core). The mobile version and agency app stay standalone.",
  "scripts": {
    "dev": "pnpm --filter crm-web dev",
    "build": "pnpm --filter crm-web build",
    "preview": "pnpm --filter crm-web preview",
    "typecheck": "pnpm -r typecheck"
  }
}
