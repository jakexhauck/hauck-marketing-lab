<section class="phead">
  <div class="wrap">
    <p class="crumbs"><a href="{{ routes.root_url }}">Home</a> / {{ page.title }}</p>
    <h1>{{ page.title }}</h1>
  </div>
</section>

<div class="sect">
  <div class="wrap prose rte" data-rise>
    {{ page.content }}
  </div>
</div>

{% schema %}
{ "name": "Page content" }
{% endschema %}
