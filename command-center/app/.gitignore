node_modules
dist
dist-ssr
*.local
.vite
.wrangler

# local secrets (wrangler pages dev)
.dev.vars

# editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# Backups written by scripts/purge-landline-leads.mjs. Real business contact
# data, and a hard delete means the file is the only copy, so it is kept on disk
# and out of the repo.
scripts/landline-leads-*.json
