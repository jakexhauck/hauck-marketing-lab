# Sprints 1-3: Stability, Client Value, Polish (local-first)

Branch: `sprints-1-3-local`. Nothing pushes until Jake reviews on localhost.

Agreed scope from the 2026-08-22 review session. No Anthropic API usage anywhere;
the dead coach surface is REMOVED instead.

## Sprint 1: Stop things breaking

| # | Change | Files |
|---|--------|-------|
| 1.1 | Error log table + best-effort writer; middleware and webhook side effects record failures | `supabase/migrations/0119_error_log.sql`, `functions/lib/errorLog.ts` + test, `functions/api/_middleware.ts`, `functions/api/webhook.ts` |
| 1.2 | Cron heartbeat table; ads/calendar/dialer syncs bump it; health probe reads it so the EXISTING snapshot-diff push alerts when a worker stops | `supabase/migrations/0120_cron_heartbeats.sql`, `functions/lib/cronHeartbeat.ts` + test, `functions/api/admin/ads/sync.ts`, `functions/api/admin/calendar/sync.ts`, `functions/api/admin/cold-call/sync.ts`, `functions/api/admin/connections/health.ts` |
| 1.3 | Owner-session revocation: `tenants.session_version`, `v` claim in token, checked in middleware after tenant load | `supabase/migrations/0121_tenant_session_version.sql`, `functions/lib/session.ts` + test, `functions/api/auth/login.ts`, `_middleware.ts` |
| 1.4 | Remove the phantom coach surface (public paths, overlay CORS, COACH_* / ANTHROPIC_* env) | `_middleware.ts`, `functions/lib/env.ts` |
| 1.5 | ~~Stop storing plaintext intake passwords~~ REVERSED by Jake's decision 2026-08-22: the admin password read-back stays (migration 0081 reaffirmed). No migration ships for this item | n/a |
| 1.6 | Login rate limiter degradation becomes observable (memory fallback still open by design, but now logged + counted) | `functions/lib/ratelimit.ts` |
| 1.7 | TEST/live hazard: TopBar brand from appBrand not hardcoded; DemoBanner exit goes to `/admin`; test-mode login warns when env location differs from the tenant row's | `src/components/TopBar.tsx`, `src/components/DemoBanner.tsx`, `functions/api/auth/login.ts` |
| 1.8 | Admin errors API for the UI (`GET /api/admin/errors`) | `functions/api/admin/errors.ts` |

## Sprint 2: Things clients see

| # | Change | Files |
|---|--------|-------|
| 2.1 | Monthly client report: server builds one JSON report (Meta spend + GA4 + revenue join), client-facing `/reports` page renders it print-ready, Resend emails the link | `functions/api/reports/monthly.ts`, `src/routes/reports/*`, nav row (capability: paidads), admin send button in cockpit OverviewTab |
| 2.2 | Speed-to-lead: first-response latency per lead from activity_log pairs, shown as a band on Lead Tracker + a column in tracker data | `functions/lib/speedToLead.ts` + test, `functions/api/ads/tracker.ts` or lead feeds, tracker UI band |

## Sprint 3: Look and feel

| # | Change | Files |
|---|--------|-------|
| 3.1 | One KpiTile, one EmptyState, one StatusPill; legacy imports migrated | `src/components/ui/KpiTile.tsx`, `ui/Feedback.tsx`, callers |
| 3.2 | A11y floor: focus rings on Sidebar/BottomNav, 44px targets on bell/quick actions, Segmented arrow keys, friendly error text component | `Sidebar.tsx`, `BottomNav.tsx`, `NotificationBell.tsx`, `ContactsDesktop.tsx`, `Segmented.tsx`, error states |
| 3.3 | Lead Tracker density pass: summary band (spend/CPL/bookings/revenue + trend), fix sort order, color-coded ROAS vs target, honest zero labels | `AdsLeadTracker.tsx`, `DashboardSheet.tsx`, `adsInsights.ts` |
| 3.4 | GlobalSearch becomes a real command palette (Cmd+K): pages + clients + leads, keyboard driven | `src/components/desktop/CommandPalette.tsx`, `Shell.tsx`, page state handoff |
| 3.5 | Morning briefing: computed inside the existing health-cron scheduled call once per day in window, pushed to admin devices; uses heartbeats + errors + watch diff + ad spend | `functions/api/admin/connections/health.ts`, `functions/lib/morningBriefing.ts` + test |

## Explicitly deferred (not forgotten)

- Full retirement of the five scoped CSS dialects (`.pk-kit` etc). Too wide for this pass; indigo reconciliation only.
- Migrating all ~10 dialogs onto one Modal primitive. Pattern set on 2-3 dialogs.
- Renaming TEST_* env vars: needs Doppler + Cloudflare secret renames on ship day.
- Dropping destructive columns beyond password_plain.

## Verification per sprint

1. `pnpm typecheck`
2. `pnpm test`
3. Dev servers up (`pnpm dev:full` needs Doppler vars; `pnpm dev` alone for UI)
4. Screenshots of touched surfaces
5. Migrations applied via `pnpm db:migrate` ONLY after Jake sees the plan output; additive ones are safe, 0122 is destructive and called out.
