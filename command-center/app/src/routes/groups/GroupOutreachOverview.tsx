import SectionComingSoon from "../../components/SectionComingSoon";
import { GROUP_OUTREACH_TABS } from "../../lib/pageTabs";

export default function GroupOutreachOverview() {
  return <SectionComingSoon tabs={GROUP_OUTREACH_TABS} title="Facebook group outreach" />;
}
